{
  "name": "veracity-bert-liar",
  "version": "1.0.0",
  "private": true,
  "description": "Misinformation detection web app — FastAPI backend + React/TypeScript frontend.",
  "scripts": {
    "install:all": "npm install && npm --prefix frontend install && python3 -m pip install -r backend/requirements.txt",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "python3 -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8000 --reload",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test:backend": "python3 -m pytest backend -q"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
