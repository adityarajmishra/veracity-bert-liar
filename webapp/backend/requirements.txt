# NOTE: torch is installed separately in the Dockerfile from the CPU-only wheel
# index (https://download.pytorch.org/whl/cpu) to avoid pulling ~2 GB of CUDA
# GPU libraries. For local dev, `pip install torch` is fine.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
transformers==4.30.0
huggingface_hub>=0.20,<1.0
numpy
